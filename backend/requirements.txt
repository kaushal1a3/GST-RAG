# GST RAG System – Production Dependencies (Vercel Serverless Ready)

# ── Core Data & Vector Search ─────────────────────────────────────────────
numpy>=1.26,<3.0
qdrant-client>=1.9.0
fastembed>=0.3.0                       # Lightweight ONNX query embedding (~40MB, no PyTorch required)

# ── Keyword Search & Utilities ────────────────────────────────────────────
rank-bm25>=0.2.2                       # BM25Okapi – pure Python
python-dotenv>=1.0.0                 # Environment variable loader
tqdm>=4.66

# ── LLM Clients & LangChain Agent ─────────────────────────────────────────
google-genai>=0.3                    # Official Google GenAI SDK (replaces deprecated google-generativeai)
requests>=2.31                       # HTTP client
langchain>=0.3                       # LangChain agent framework
langchain-core>=1.0
langchain-google-genai>=2.0          # Gemini via LangChain
langchain-anthropic>=0.3             # Claude via LangChain
langchain-community>=0.3

# ── API Layer ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0
